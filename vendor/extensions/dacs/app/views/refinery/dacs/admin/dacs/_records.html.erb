<%= render 'refinery/admin/search_header', :url => refinery.dacs_admin_dacs_path %>
<div class='pagination_container'>
  <% if @dacs.any? %>
    <%= render 'dacs' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
