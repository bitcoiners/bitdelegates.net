<%= form_for [refinery, :dacs_admin, @dac] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @dac,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :name -%>
    <%= f.text_field :name, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:description],
                :object => "dacs/dac" -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.dacs.admin.dacs.dac'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @dac.name) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
